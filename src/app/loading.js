import { Skeleton } from '@/components/ui/skeleton';
import React from 'react';

const loading = () => {
    return (
        <div>
            <Skeleton></Skeleton>
        </div>
    );
};

export default loading;